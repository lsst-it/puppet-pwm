---
fixtures:
  forge_modules:
    archive:
      repo: "puppet/archive"
      ref: "7.0.0"
    stdlib:
      repo: "puppetlabs/stdlib"
      ref: "9.3.0"
    systemd:
      repo: "puppet/systemd"
      ref: "6.0.0"

